<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || "Update Password" %></title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #000;
      color: #fff;
      font-family: Arial, sans-serif;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .form-container {
      background: #111;
      padding: 30px;
      border-radius: 10px;
      width: 100%;
      max-width: 400px;
      box-shadow: 0 0 15px rgba(0,0,0,0.6);
    }

    h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #fff;
    }

    label {
      color: #fff;
      margin-top: 10px;
    }

    input {
      background: #222;
      color: #fff;
      border: 1px solid #444;
    }

    input:focus {
      outline: none;
      border-color: #1db954;
      box-shadow: 0 0 5px #1db954;
    }

    .btn-custom {
      width: 100%;
      margin-top: 20px;
      background: #1db954;
      border: none;
      color: #fff;
      font-weight: bold;
    }

    .btn-custom:hover {
      background: #17a84a;
    }

    .msg {
      text-align: center;
      margin-bottom: 15px;
      color: #ff5555;
    }
  </style>
</head>
<body>

  <div class="form-container">
    <h2>Update Password</h2>

    <% if (msg) { %>
      <div class="msg"><%= msg %></div>
    <% } %>

    <!-- ✅ Fixed: only showing email -->
    <p class="text-center mb-3">Your email: <strong><%= email %></strong></p>

    <form action="/admin/reset-password" method="POST">
      <input type="hidden" name="token" value="<%= token %>">

      <div class="mb-3">
        <label for="password">New Password</label>
        <input type="text" id="password" name="password" class="form-control" required>
      </div>

      <div class="mb-3">
        <label for="confirmPassword">Confirm Password</label>
        <input type="text" id="confirmPassword" name="confirm" class="form-control" required>
      </div>

      <button type="submit" class="btn btn-custom">Update Password</button>
    </form>
  </div>

</body>
</html>
