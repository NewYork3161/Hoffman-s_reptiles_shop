<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title || "Check Your Email" %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body {
      background:#111;
      color:#fff;
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      margin:0;
    }
    .card {
      background:#222;
      max-width:520px;
      width:100%;
      border:1px solid #444;
      border-radius:12px;
      padding:2rem;
      box-shadow:0 0 15px rgba(0,0,0,0.6);
      text-align:center;
    }
    .page-title {
      color:#fff;
      font-size:1.6rem;
      font-weight:600;
      margin-bottom:1rem;
      text-shadow:0 0 6px rgba(255,255,255,.4);
    }
    .note {
      color:#bbb;
      font-size:1rem;
      margin-bottom:1.5rem;
    }
    .btn-primary {
      background:#0d6efd;
      border:none;
    }
    .btn-primary:hover {
      background:#0b5ed7;
    }
  </style>
</head>
<body>
  <div class="card shadow">
    <h2 class="page-title">Check Your Email</h2>
    
    <p class="note">
      We have sent a link to your email address 
      <% if (typeof email !== 'undefined' && email) { %>
        <strong><%= email %></strong>.
      <% } else { %>
        provided during registration or recovery.
      <% } %>
    </p>

    <a class="btn btn-primary w-100" href="/admin/login">Back to Login</a>
  </div>
</body>
</html>
