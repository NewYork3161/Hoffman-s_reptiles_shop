<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= animal.name %> - Hoffman's Reptiles</title>
 <link rel="icon" type="image/png" href="/Images/Logo.png" />
  <link rel="shortcut icon" type="image/png" href="/Images/Logo.png" />

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>

  <!-- Shared stylesheet -->
  <link rel="stylesheet" href="/CSS/home_page.styles.css" />

  <style>
    :root { --green:#0a4e07; --bg:#000; --card:#0b0b0b; }
    body { margin:0; background:var(--bg); color:#fff; font-family:Arial, Helvetica, sans-serif; padding-top:76px; }

    /* HERO IMAGE: show the whole image (no crop, no border line) */
    .detail-hero {
      width:100%;
      background:#000;
      display:flex;
      justify-content:center;
      align-items:center;
      padding:0;
    }
    .detail-hero img {
      width:100%;
      max-height:70vh;
      object-fit:contain;   /* show full image */
      display:block;
      background:#000;
    }

    .detail-wrap { max-width: 920px; margin: 0 auto; padding: 28px 16px 56px; text-align:center; }

    .animal-title { color:var(--green); font-weight:800; font-size:2.1rem; margin:16px 0 12px; }

    .meta-row { display:flex; gap:12px; justify-content:center; align-items:center; margin-bottom:14px; }
    .price { color:var(--green); font-weight:800; font-size:1.25rem; }
    .availability { padding:6px 12px; border-radius:999px; font-size:.95rem; }
    .availability.yes { background:rgba(0,128,0,.25); color:#b7ffb7; border:1px solid rgba(0,128,0,.35); }
    .availability.no  { background:rgba(128,0,0,.25); color:#ffb7b7; border:1px solid rgba(128,0,0,.35); }

    /* Description box: centered, wraps long words, expands height as needed */
    .description {
      max-width: 820px;
      margin: 18px auto 0;
      color: var(--green);        /* dark green to match title/price */
      background: var(--card);
      border: 1px solid #1b1b1b;
      border-radius: 10px;
      padding: 16px 18px;
      line-height: 1.6;
      text-align: center;

      /* Wrapping fixes */
      word-wrap: break-word;      /* legacy */
      overflow-wrap: break-word;  /* modern */
      white-space: pre-wrap;      /* keep manual line breaks but allow wrap */
    }

    .back-link { display:inline-block; margin-top:22px; color:var(--green); text-decoration:none; }
    .back-link:hover { text-decoration:underline; }
  </style>
</head>
<body>
  <!-- Navbar -->
  <%- include('partials/navbar') %>

  <!-- Hero (full image, no crop) -->
  <div class="detail-hero">
    <img src="<%= animal.image %>" alt="<%= animal.name %>">
  </div>

  <!-- Content -->
  <div class="detail-wrap">
    <h1 class="animal-title"><%= animal.name %></h1>

    <div class="meta-row">
      <span class="price">$<%= animal.price %></span>
      <span class="availability <%= animal.available ? 'yes' : 'no' %>">
        <%= animal.available ? 'Available' : 'Not Available' %>
      </span>
    </div>

    <% if (animal.description) { %>
      <div class="description"><%= animal.description %></div>
    <% } %>

    <a href="/animals" class="back-link">← Back to Animals</a>
  </div>

  <!-- Footer -->
  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
