<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Message Sent - Hoffman's Reptiles</title>

  <!-- CSS -->
  <link rel="stylesheet" href="/CSS/home_page.styles.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    body {
      background-color: #000;
      color: #28a745;
      margin: 0;
      font-family: Arial, Helvetica, sans-serif;
    }
    .confirmation-box {
      background-color: #1a1a1a;
      border-radius: 12px;
      padding: 40px;
      margin: 100px auto;
      text-align: center;
      max-width: 600px;
      box-shadow: 0 0 15px rgba(0,0,0,.6);
    }
    .confirmation-box h2 {
      font-size: 2rem;
      color: #28a745;
      margin-bottom: 20px;
    }
    .confirmation-box p {
      font-size: 1.2rem;
      color: #ccc;
      margin-bottom: 30px;
    }
    .btn-group {
      display: flex;
      justify-content: center;
      gap: 15px;
      flex-wrap: wrap;
    }
    .btn-success {
      background-color: #28a745;
      border: none;
      padding: 12px 20px;
      font-weight: bold;
    }
    .btn-success:hover {
      background-color: #218838;
      color: #fff;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <%- include('partials/navbar') %>

  <!-- Confirmation Box -->
  <div class="confirmation-box">
    <h2>Thanks for contacting Hoffman's Reptiles!</h2>
    <p>We're glad you reached out. We'll get back to you as soon as we can.</p>

    <div class="btn-group">
      <a href="/contact" class="btn btn-success">Back to Contact</a>
      <a href="https://www.google.com/maps?q=2359+Concord+Blvd,+Concord,+CA+94520" target="_blank" class="btn btn-success">Map</a>
      <a href="tel:+19256719106" class="btn btn-success">Call (925) 671-9106</a>
    </div>
  </div>

  <!-- Footer -->
  <%- include('partials/footer') %>
</body>
</html>
