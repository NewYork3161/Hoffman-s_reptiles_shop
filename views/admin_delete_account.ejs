<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title || 'Delete Admin Account' %></title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>

  <style>
    body {
      background:#111;
      color:#fff;
      min-height:100vh;
      display:flex;
      align-items:center;
      justify-content:center;
      margin:0;
      font-family: Arial, sans-serif;
    }
    .card {
      background:#222;
      max-width:520px;
      width:100%;
      border:1px solid #444;
      border-radius:12px;
      box-shadow: 0 0 15px rgba(0,0,0,0.6);
    }
    .page-title {
      color:#fff;
      font-size:1.6rem;
      font-weight:600;
      text-align:center;
      text-shadow:0 0 6px rgba(255,255,255,.35);
    }
    .form-label { 
      color:#fff; 
      font-weight:500; 
    }
    .form-control {
      background:#2b2b2b;
      border:1px solid #555;
      color:#f1f1f1;
    }
    .form-control:focus {
      background:#2b2b2b;
      color:#fff;
      border-color:#0d6efd;
      box-shadow:0 0 5px #0d6efd;
    }
    .btn-primary { background:#0d6efd; border:none; }
    .btn-primary:hover { background:#0b5ed7; }
    .btn-danger { font-weight:700; }
    .note { color:#ccc; font-size:.9rem; }
    .divider { border-top:1px solid #444; margin:1rem 0; }

    /* ✅ Fix checkbox label visibility */
    .form-check-label {
      color: #f1f1f1;
      font-size: 0.95rem;
    }
    .form-check-input {
      background-color: #333;
      border-color: #666;
    }
    .form-check-input:checked {
      background-color: #dc3545; /* red checkmark */
      border-color: #dc3545;
    }
  </style>
</head>
<body>
  <div class="card p-4">
    <h2 class="page-title mb-3">Delete Admin Account</h2>

    <% if (typeof msg !== 'undefined' && msg) { %>
      <div class="alert alert-warning py-2"><%= msg %></div>
    <% } %>

    <p class="note mb-3">
      This will permanently remove your admin account and all associated login access.
      You can re-register later if needed. This action cannot be undone.
    </p>

    <!-- Form: delete account -->
    <form action="/admin/delete-account" method="POST" novalidate>
      <div class="mb-3">
        <label for="firstName" class="form-label">First Name</label>
        <input id="firstName" name="firstName" type="text" class="form-control" required />
      </div>

      <div class="mb-3">
        <label for="lastName" class="form-label">Last Name</label>
        <input id="lastName" name="lastName" type="text" class="form-control" required />
      </div>

      <div class="mb-3">
        <label for="phone" class="form-label">Phone Number</label>
        <input id="phone" name="phone" type="tel" class="form-control"
               pattern="^[0-9\\-\\+\\s\\(\\)]{7,20}$"
               title="Enter a valid phone number" required />
      </div>

      <div class="mb-3">
        <label for="email" class="form-label">Email</label>
        <input id="email" name="email" type="email" class="form-control" required />
      </div>

      <div class="mb-3">
        <label for="password" class="form-label">Current Password</label>
        <input id="password" name="password" type="password" class="form-control" required />
      </div>

      <div class="form-check my-3">
        <input class="form-check-input" type="checkbox" value="1" id="confirmDelete">
        <label class="form-check-label" for="confirmDelete">
          I understand this will permanently delete my account.
        </label>
      </div>

      <div class="d-grid gap-2">
        <button id="deleteBtn" class="btn btn-danger" type="submit" disabled>Delete My Account</button>
        <a class="btn btn-primary" href="/admin/dashboard">Cancel / Back to Dashboard</a>
      </div>
    </form>

    <div class="divider"></div>
    <div class="text-center">
      <a class="text-decoration-none text-info" href="/admin/login">Go to Admin Login</a>
    </div>
  </div>

  <script>
    // Enable Delete button only when checkbox is checked
    (function () {
      const cb = document.getElementById('confirmDelete');
      const btn = document.getElementById('deleteBtn');
      cb.addEventListener('change', () => { btn.disabled = !cb.checked; });
    })();
  </script>
</body>
</html>
