<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="Full inventory list of reptiles at Hoffman's Reptile Shop. Find available snakes, lizards, tortoises, and exotic pets in Walnut Creek, CA." />
  <meta name="keywords" content="reptile inventory, reptiles for sale Walnut Creek, snakes, lizards, tortoises, turtles, exotic pets Bay Area" />

  <title>Monitor Lizard Inventory</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    :root{
      --navbar-offset: 70px;   /* match your fixed navbar height */
      --green: #1DB954;        /* bright readable green */
      --thumb-w: 360px;        /* same size as detail page */
    }

    /* Full black page with sticky footer */
    html, body { height: 100%; }
    body{
      margin: 0;
      background: #000;        /* all black */
      color: #fff;
      display: flex;
      flex-direction: column;  /* lets footer stick to bottom */
      min-height: 100vh;
      padding-top: var(--navbar-offset); /* offset the fixed navbar */
      font-family: Arial, sans-serif;
    }

    main.page-content { flex: 1; } /* pushes footer to bottom */

    /* Title: green and spaced a bit from navbar */
    .page-title{
      color: var(--green) !important;
      margin-top: 14px;        /* extra breathing room under navbar */
      text-align: center;
    }

    /* Thumbnail image (same size/feel as other page) */
    .monitor-thumb{
      width: var(--thumb-w);
      max-width: 100%;
      height: auto;
      display: block;
      margin: 0 auto 1rem;     /* centered + space below */
      border-radius: 10px;
      box-shadow: 0 8px 24px rgba(0,0,0,0.7);
    }
  </style>
</head>
<body>
  <%- include('partials/navbar') %>

  <main class="page-content">
    <div class="container py-5">
      <!-- Page Title -->
      <h1 class="page-title mb-4">Monitor Lizard Inventory</h1>

      <!-- Asian Water Monitor thumbnail (image only) -->
      <div class="text-center mb-3">
        <img
          src="/Images/inventory_image_Asian water_monitor.png"
          alt="Asian Water Monitor"
          class="monitor-thumb"
        />
      </div>

      <!-- Button to detail page -->
      <div class="text-center mt-2">
        <a href="/asian_water_monitor" class="btn btn-success btn-lg">Asian Water Monitor</a>
      </div>
    </div>
  </main>

  <!-- Footer (will stay at the bottom because of flex layout) -->
  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
